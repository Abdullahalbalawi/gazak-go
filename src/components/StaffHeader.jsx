import React,{useState,useEffect,useRef}from"react";import{useAuth}from"@/lib/AuthContext";import{DEMO_MODE}from"@/lib/demoMode";import{Bell,LogOut,Menu,X}from"lucide-react";import{Link,useNavigate}from"react-router-dom";
const ROLE_LABELS={customer:"العميل",distributor:"الموزع",driver:"السائق",admin:"المدير"};const ROLE_HOMES={customer:"/",distributor:"/distributor",driver:"/driver",admin:"/admin"};const NAV_BY_ROLE={distributor:[{to:"/distributor",label:"الطلبات"},{to:"/distributor/custody",label:"عهدتي"}],driver:[{to:"/driver",label:"طلباتي"}],admin:[{to:"/admin",label:"الرئيسية"},{to:"/admin/orders",label:"الطلبات"},{to:"/admin/products",label:"المنتجات"},{to:"/admin/inventory",label:"المخزون"},{to:"/admin/users",label:"المستخدمون"}]};
export default function StaffHeader({title}){const{user,logout,switchDemoRole}=useAuth();const navigate=useNavigate();const[unread,setUnread]=useState(0),[menuOpen,setMenuOpen]=useState(false),ref=useRef(null);const role=user?.role||"customer",nav=NAV_BY_ROLE[role]||[];const fetchUnread=async()=>{if(DEMO_MODE||!user?.id){setUnread(0);return}const{count}=await (await import("@/lib/supabaseClient")).supabase.from("notifications").select("id",{count:"exact",head:true}).eq("user_id",user.id).eq("read",false);setUnread(count||0)};useEffect(()=>{fetchUnread();if(DEMO_MODE)return undefined;ref.current=setInterval(fetchUnread,15000);return()=>clearInterval(ref.current)},[user?.id]);const handleLogout=async()=>{await logout();navigate(DEMO_MODE?"/":"/login")};const changeRole=e=>{const next=e.target.value;if(!DEMO_MODE||!switchDemoRole)return;switchDemoRole(next);navigate(ROLE_HOMES[next]);};return <header className="sticky top-0 z-40 bg-white border-b border-border"><div className="max-w-3xl mx-auto px-4 h-14 flex items-center justify-between"><div className="flex items-center gap-3">{nav.length>1&&<button className="md:hidden" onClick={()=>setMenuOpen(v=>!v)}>{menuOpen?<X className="w-5 h-5"/>:<Menu className="w-5 h-5"/>}</button>}<div><h1 className="font-bold text-base">{title||ROLE_LABELS[role]||"لوحة التحكم"}</h1>{DEMO_MODE&&<p className="text-[10px] text-muted-foreground">حساب تجريبي</p>}</div></div><div className="flex items-center gap-2">{DEMO_MODE&&<select value={role} onChange={changeRole} className="h-9 max-w-[105px] rounded-lg border bg-white px-2 text-xs" aria-label="تبديل الدور التجريبي">{Object.entries(ROLE_LABELS).map(([key,label])=><option key={key} value={key}>{label}</option>)}</select>}<Link to="/notifications" className="relative p-2 rounded-lg hover:bg-muted"><Bell className="w-5 h-5"/>{unread>0&&<span className="absolute top-1 left-1 bg-red-500 text-white text-[10px] font-bold rounded-full w-4 h-4 flex items-center justify-center">{unread>9?"9+":unread}</span>}</Link><button onClick={handleLogout} className="p-2 rounded-lg hover:bg-muted" aria-label="تسجيل الخروج"><LogOut className="w-5 h-5"/></button></div></div>{nav.length>1&&<nav className="hidden md:flex border-t px-4 max-w-3xl mx-auto">{nav.map(i=><Link key={i.to} to={i.to} className="px-4 py-2 text-sm font-medium text-muted-foreground hover:text-foreground">{i.label}</Link>)}</nav>}{menuOpen&&<nav className="md:hidden border-t bg-white px-4 py-2">{nav.map(i=><Link key={i.to} to={i.to} onClick={()=>setMenuOpen(false)} className="block py-2 text-sm">{i.label}</Link>)}</nav>}</header>}
