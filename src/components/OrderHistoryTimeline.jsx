import React,{useEffect,useState} from "react";
import {supabase} from "@/lib/supabaseClient";
import {DEMO_MODE,getDemoOrder} from "@/lib/demoMode";
import {STATUS_LABELS_AR} from "@/lib/orderStatus";
import {History} from "lucide-react";
export default function OrderHistoryTimeline({orderId,order}){const[history,setHistory]=useState([]);const[loading,setLoading]=useState(true);useEffect(()=>{if(!orderId)return;let active=true;if(DEMO_MODE){const demo=order?.history||getDemoOrder(orderId)?.history||[];if(active)setHistory(demo);setLoading(false);return undefined}const fetchHistory=async()=>{try{const{data,error}=await supabase.from("order_history").select("*").eq("order_id",orderId).order("created_at",{ascending:true}).limit(50);if(error)throw error;if(active)setHistory(data||[])}catch(e){console.error(e)}finally{if(active)setLoading(false)}};fetchHistory();const channel=supabase.channel(`order-history-${orderId}`).on("postgres_changes",{event:"INSERT",schema:"public",table:"order_history",filter:`order_id=eq.${orderId}`},payload=>setHistory(prev=>prev.some(i=>i.id===payload.new.id)?prev:[...prev,payload.new])).subscribe();return()=>{active=false;supabase.removeChannel(channel)}},[orderId,order]);if(loading||history.length===0)return null;return <div className="bg-white rounded-2xl border border-border p-4 mb-3"><h3 className="font-semibold text-sm text-foreground mb-3 flex items-center gap-1.5"><History className="w-4 h-4 text-muted-foreground"/>سجل الحركة</h3><div className="space-y-2.5">{history.map(h=><div key={h.id} className="flex items-start gap-3"><div className="w-2.5 h-2.5 rounded-full bg-primary flex-shrink-0 mt-1.5"/><div className="flex-1"><div className="flex justify-between items-center gap-2"><span className="text-sm font-medium text-foreground">{STATUS_LABELS_AR[h.new_status]||h.new_status}</span><span className="text-xs text-muted-foreground">{new Date(h.created_at).toLocaleString("ar-SA")}</span></div>{h.performed_by_name&&<p className="text-xs text-muted-foreground mt-0.5">بواسطة: {h.performed_by_name}{h.performed_by_role&&h.performed_by_role!=="customer"&&` (${h.performed_by_role})`}</p>}{h.note&&<p className="text-xs text-muted-foreground mt-1 bg-muted rounded-lg px-2 py-1">{h.note}</p>}</div></div>)}</div></div>}
