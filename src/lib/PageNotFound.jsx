import { useLocation } from 'react-router-dom';
import { useAuth } from '@/lib/AuthContext';
export default function PageNotFound(){const location=useLocation();const{user}=useAuth();const pageName=location.pathname.substring(1);return <div className="min-h-screen flex items-center justify-center p-6 bg-slate-50"><div className="max-w-md w-full text-center space-y-6"><h1 className="text-7xl font-light text-slate-300">404</h1><div className="h-0.5 w-16 bg-slate-200 mx-auto"/><div><h2 className="text-2xl font-medium text-slate-800">الصفحة غير موجودة</h2><p className="text-slate-600 mt-3">الصفحة <span className="font-medium">"{pageName}"</span> غير موجودة في التطبيق.</p></div>{user?.role==='admin'&&<div className="p-4 bg-slate-100 rounded-lg border text-sm text-slate-600">هذه الصفحة غير معرفة في مسارات التطبيق الحالية.</div>}<button onClick={()=>window.location.href='/'} className="px-4 py-2 text-sm font-medium bg-white border rounded-lg">العودة للرئيسية</button></div></div>}
