import React, { useState, useEffect } from "react";
import { supabase } from "@/lib/supabaseClient";
import StaffHeader from "@/components/StaffHeader";
import StatusBadge from "@/components/StatusBadge";
import OrderHistoryTimeline from "@/components/OrderHistoryTimeline";
import { ORDER_STATUSES, STATUS_LABELS_AR } from "@/lib/orderStatus";
import ReturnExchangeDialog from "@/components/ReturnExchangeDialog";
import { Loader2, ChevronDown, ChevronUp, MapPin, RotateCcw } from "lucide-react";
import { toast } from "@/components/ui/use-toast";

export default function AdminOrders() {
  const [orders,setOrders]=useState([]); const [drivers,setDrivers]=useState([]); const [loading,setLoading]=useState(true); const [expanded,setExpanded]=useState(null); const [selected,setSelected]=useState({}); const [busy,setBusy]=useState(null); const [ret,setRet]=useState({open:false,order:null});
  const load=async()=>{const [{data:o,error:oe},{data:d,error:de}]=await Promise.all([supabase.from("orders").select("*, order_items(*)").order("created_at",{ascending:false}).limit(200),supabase.from("profiles").select("id,full_name,phone,active").eq("role","driver").order("created_at")]);if(oe) console.error(oe);if(de) console.error(de);setOrders(o||[]);setDrivers((d||[]).filter(x=>x.active));setLoading(false);};
  useEffect(()=>{load();const c=supabase.channel("admin-orders").on("postgres_changes",{event:"*",schema:"public",table:"orders"},load).subscribe();return()=>{supabase.removeChannel(c)};},[]);
  const assign=async(id,driverId)=>{if(!driverId)return toast({title:"اختر سائقاً أولاً",variant:"destructive"});setBusy(id);try{const {data,error}=await supabase.rpc("assign_order_smart",{p_order_id:id,p_driver_id:driverId});if(error)throw error;setOrders(x=>x.map(o=>o.id===id?{...o,...data}:o));toast({title:"تم إسناد الطلب"});}catch(e){toast({title:"تعذر الإسناد",description:e.message,variant:"destructive"})}finally{setBusy(null)}};
  const transition=async(id,status)=>{setBusy(id+status);try{const {data,error}=await supabase.rpc("transition_order",{p_order_id:id,p_new_status:status,p_note:"Admin manual transition"});if(error)throw error;setOrders(x=>x.map(o=>o.id===id?{...o,...data}:o));}catch(e){toast({title:"فشل تغيير الحالة",description:e.message,variant:"destructive"})}finally{setBusy(null)}};
  return <div className="min-h-screen bg-gray-50"><StaffHeader title="إدارة الطلبات"/><div className="max-w-3xl mx-auto px-4 py-4">{loading?<div className="flex justify-center py-12"><Loader2 className="w-6 h-6 animate-spin"/></div>:<div className="space-y-3">{orders.map(o=>{const open=expanded===o.id;return <div key={o.id} className="bg-white rounded-2xl border border-border overflow-hidden"><button onClick={()=>setExpanded(open?null:o.id)} className="w-full p-4 flex justify-between text-right"><div><div className="flex gap-2 items-center"><span className="font-mono text-xs text-muted-foreground">#{o.id.slice(-8).toUpperCase()}</span><StatusBadge status={o.status}/></div><p className="font-semibold mt-1">{o.customer_name}</p><p className="text-xs text-muted-foreground">{o.total} ر.س · {new Date(o.created_at).toLocaleString("ar-SA")}</p></div>{open?<ChevronUp/>:<ChevronDown/>}</button>{open&&<div className="border-t p-4 space-y-3"><div className="text-sm space-y-1"><p>{o.customer_phone}</p><div className="flex gap-1 text-muted-foreground"><MapPin className="w-4 h-4"/><span>{o.address}</span></div></div><div className="bg-gray-50 rounded-xl p-3 space-y-1">{o.order_items?.map(i=><div key={i.id} className="flex justify-between text-sm"><span>{i.product_name} × {i.quantity}</span><span>{i.total} ر.س</span></div>)}<div className="border-t pt-1 flex justify-between font-bold"><span>الإجمالي</span><span>{o.total} ر.س</span></div></div><OrderHistoryTimeline orderId={o.id}/>{o.status==="READY"&&<div className="bg-cyan-50 border border-cyan-200 rounded-xl p-3"><p className="font-semibold text-sm mb-2">Smart Dispatch</p><div className="flex gap-2"><select value={selected[o.id]||""} onChange={e=>setSelected({...selected,[o.id]:e.target.value})} className="flex-1 h-10 rounded-lg border bg-white px-2 text-sm"><option value="">اختيار السائق</option>{drivers.map(d=><option key={d.id} value={d.id}>{d.full_name||d.phone||d.id.slice(0,8)}</option>)}</select><button disabled={busy===o.id} onClick={()=>assign(o.id,selected[o.id])} className="px-4 rounded-lg bg-primary text-primary-foreground">{busy===o.id?<Loader2 className="w-4 h-4 animate-spin"/>:"إسناد"}</button></div></div>}{["NEW","ACCEPTED","PREPARING"].includes(o.status)&&<div className="grid grid-cols-3 gap-2">{[{s:"ACCEPTED",t:"قبول"},{s:"PREPARING",t:"تجهيز"},{s:"READY",t:"جاهز"}].map(x=><button key={x.s} onClick={()=>transition(o.id,x.s)} disabled={busy===o.id+x.s} className="h-10 rounded-lg border text-sm">{busy===o.id+x.s?<Loader2 className="w-4 h-4 animate-spin mx-auto"/>:x.t}</button>)}</div>}{o.status==="DELIVERED"&&<button onClick={()=>setRet({open:true,order:{...o,items:o.order_items}})} className="w-full h-10 rounded-lg border flex items-center justify-center gap-2"><RotateCcw className="w-4 h-4"/>مرتجع / استبدال</button>}</div>}</div>})}</div>}{!loading&&orders.length===0&&<p className="text-center text-muted-foreground py-16">لا توجد طلبات</p>}</div><ReturnExchangeDialog order={ret.order} open={ret.open} onOpenChange={open=>setRet({...ret,open})} onDone={load}/></div>;
}
