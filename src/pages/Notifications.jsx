import React,{useEffect,useState} from "react";import {Link} from "react-router-dom";import {supabase} from "@/lib/supabaseClient";import {useAuth} from "@/lib/AuthContext";import {Bell,Check} from "lucide-react";import {toast} from "@/components/ui/use-toast";
export default function Notifications(){const{user}=useAuth();const[n,setN]=useState([]),[loading,setLoading]=useState(true);const load=async()=>{if(!user?.id)return;const{data,error}=await supabase.from("notifications").select("*").eq("user_id",user.id).order("created_at",{ascending:false}).limit(50);if(!error)setN(data||[]);setLoading(false)};useEffect(()=>{load();if(!user?.id)return;const c=supabase.channel(`notifications-${user.id}`).on("postgres_changes",{event:"INSERT",schema:"public",table:"notifications",filter:`user_id=eq.${user.id}`},load).subscribe();return()=>supabase.removeChannel(c)},[user?.id]);const read=async()=>{const{error}=await supabase.from("notifications").update({read:true}).eq("user_id",user.id).eq("read",false);if(error)toast({title:"فشل التحديث",variant:"destructive"});else{setN(x=>x.map(a=>({...a,read:true})));toast({title:"تم تحديد الكل كمقروء"})}};const home=user?.role==="customer"?"/":user?.role==="distributor"?"/distributor":user?.role==="driver"?"/driver":"/admin";return <div className="min-h-screen bg-gray-50"><header className="sticky top-0 z-40 bg-white border-b"><div className="max-w-md mx-auto px-4 h-14 flex items-center justify-between"><Link to={home} className="text-sm text-muted-foreground">رجوع</Link><h1 className="font-bold">الإشعارات</h1><button onClick={read} className="text-sm text-primary font-medium">تعليم الكل كمقروء</button></div></header><div className="max-w-md mx-auto px-4 py-4">{loading?<div className="flex justify-center py-12"><Loader2Fallback/></div>:n.length===0?<div className="flex flex-col items-center py-16 text-muted-foreground"><Bell className="w-14 h-14 mb-3"/><p>لا توجد إشعارات</p></div>:<div className="space-y-2">{n.map(x=><div key={x.id} className={`rounded-2xl border p-4 flex gap-3 ${x.read?"bg-white":"bg-primary/5 border-primary/20"}`}><div className="w-9 h-9 rounded-full flex items-center justify-center flex-shrink-0">{x.read?<Check className="w-4 h-4"/>:<Bell className="w-4 h-4"/>}</div><div><p className="font-semibold text-sm">{x.title}</p>{x.body&&<p className="text-sm text-muted-foreground">{x.body}</p>}<p className="text-xs text-muted-foreground mt-1">{new Date(x.created_at).toLocaleString("ar-SA")}</p></div></div>)}</div>}</div></div>}
function Loader2Fallback(){return <div className="w-6 h-6 border-4 border-gray-200 border-t-primary rounded-full animate-spin"/>}
